node_modules
dist
PUSH-TOKEN.txt

.autopilot-heartbeat
.autopilot-lane.lock

runner-logs/

# Session debris the RN1 gate would otherwise catch: git format-patch output
# and one-off live-bundle snapshots that agents leave lying around.
*.patch
live-bundle.js

# Screenshots the practice QA writes for a human to look at (qa-practice-shot.mjs).
qa-shots/

# local scratch
_*.mjs
qa-shots/
